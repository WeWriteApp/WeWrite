{
    "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':!README.md'",
    "buildCommand": "touch service-account-key.json && echo '{\"type\":\"service_account\",\"project_id\":\"wewrite-ccd82\"}' > service-account-key.json && npm run build",
    "env": {
        "SKIP_TYPE_CHECK": "1",
        "NODE_OPTIONS": "--max_old_space_size=4096"
    }
}