export const metadata = {
  title: 'WeWrite - User Profile',
  description: 'View user profile and collaborative writing projects',
};

export default function UserProfileLayout({ children }) {
  return children;
}
