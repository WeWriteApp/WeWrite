export const metadata = {
  title: 'WeWrite - My Groups',
  description: 'View and manage your collaborative writing groups',
};

export default function GroupsLayout({ children }) {
  return children;
}
