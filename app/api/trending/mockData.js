/**
 * Empty trending pages data for when Firebase credentials are not available
 * We no longer provide mock data to ensure only real content is shown
 */

export const generateMockTrendingPages = () => {
  // Return an empty array instead of mock trending pages
  return [];
};
